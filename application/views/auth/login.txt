<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>EVSU Payroll System Portal</title>
<link rel="icon" type="image/png" sizes="16x16" href="<?=base_url()?>assets/img/favicon.png">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

<style>
    body {
        background: linear-gradient(135deg, #f4f6f9 0%, #e9ecef 100%);
        height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        font-family: "Segoe UI", Arial, sans-serif;
    }

    .portal-container {
        width: 100%;
        max-width: 450px;
        padding: 15px;
    }

    .portal-card {
        border-radius: 10px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
        border: none;
    }

    .portal-header {
        text-align: center;
        margin-bottom: 25px;
    }

    .portal-logo {
        width: 85px;
        margin-bottom: 15px;
    }

    .portal-title {
        font-weight: 600;
        color: #7b1113;
        margin-bottom: 4px;
    }

    .portal-subtitle {
        font-size: 14px;
        color: #6c757d;
    }

    .section-title {
        font-size: 15px;
        font-weight: 600;
        color: #7b1113;
        margin-bottom: 10px;
    }

    .form-label {
        font-weight: 500;
        font-size: 14px;
    }

    .form-control {
        height: 44px;
        border-radius: 6px;
    }

    .form-control:focus {
        border-color: #7b1113;
        box-shadow: 0 0 0 0.15rem rgba(123, 17, 19, 0.15);
    }

    .btn-evsu {
        background-color: #7b1113;
        color: #fff;
        font-weight: 500;
        height: 44px;
        border-radius: 6px;
    }

    .btn-evsu:hover {
        background-color: #5e0d0f;
    }

    .divider {
        border-top: 1px solid #dee2e6;
        margin: 25px 0;
    }

    .switch-link {
        font-size: 13px;
        color: #7b1113;
        text-decoration: underline;
        cursor: pointer;
        background: none;
        border: none;
        padding: 0;
    }

    #verifySection {
        display: none;
    }

    .footer-text {
        font-size: 12px;
        color: #6c757d;
        margin-top: 20px;
    }
</style>
</head>
<body>

<div class="portal-container">
    <div class="card portal-card p-4">

        <div class="portal-header">
            <img src="<?= base_url('assets/img/favicon.png') ?>" 
                 class="portal-logo" 
                 alt="EVSU Logo">
            <h5 class="portal-title">Eastern Visayas State University</h5>
            <div class="portal-subtitle">
                Human Resource and Financial Management System
            </div>
        </div>
        <?php if($this->session->flashdata('error')): ?>
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <?= $this->session->flashdata('error'); ?>
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        <?php endif; ?>
        <!-- AUTHORIZED LOGIN SECTION -->
        <div id="loginSection">
            <div class="section-title">Authorized Personnel Login</div>

            <form method="post" action="<?= base_url('welcome/login') ?>">
                <input type="hidden" 
                       name="reg_token" 
                       value="<?= $this->session->userdata('reg_token'); ?>">

                <div class="mb-3">
                    <label class="form-label">Institutional Email Address</label>
                    <input type="email" 
                           name="email" 
                           class="form-control" 
                           required>
                </div>

                <div class="mb-4">
                    <label class="form-label">Password</label>
                    <input type="password" 
                           name="password" 
                           class="form-control" 
                           required>
                </div>

                <div class="d-grid mb-2">
                    <button type="submit" class="btn btn-evsu">
                        Sign In
                    </button>
                </div>
            </form>

            <div class="text-center mt-2">
                <button class="switch-link" onclick="showVerifySection()">
                    Access Payroll Verification
                </button>
            </div>
        </div>

        <!-- PAYROLL VERIFICATION SECTION -->
        <div id="verifySection">

            <div class="section-title">Payroll Verification Portal</div>
            <p class="text-muted" style="font-size:13px;">
                Verify the authenticity and status of a payroll record by scanning the official QR code
                or entering the Payroll Token ID.
            </p>

            <!-- OPTION BUTTONS -->
            <div class="d-grid gap-2 mb-3">
                <button class="btn btn-outline-primary" onclick="showTokenInput()">
                    Enter Payroll Token ID
                </button>

                <button class="btn btn-outline-success" onclick="showScanner()">
                    Scan QR Code
                </button>
            </div>

            <!-- TOKEN INPUT FORM -->
            <div id="tokenForm" style="display:none;">
                <form method="post" action="<?= base_url('verify/verify_token') ?>">
                    <div class="mb-3">
                        <label class="form-label">Payroll Token ID</label>
                        <input type="text" 
                            name="token_id" 
                            class="form-control text-uppercase text-center" 
                            placeholder="XXX-XXX-XXX"
                            maxlength="11"
                            required>
                    </div>

                    <div class="d-grid mb-2">
                        <button type="submit" class="btn btn-evsu">
                            Verify Record
                        </button>
                    </div>
                </form>
            </div>

            <!-- QR SCANNER AREA -->
            <div id="qrScanner" style="display:none;">
                <div id="reader" style="width:100%;"></div>

                <div class="text-center mt-3">
                    <small class="text-muted">
                        Position the QR code inside the frame to scan automatically.
                    </small>
                </div>
            </div>

            <div class="text-center mt-3">
                <button class="switch-link" onclick="showLoginSection()">
                    Return to Login
                </button>
            </div>

        </div>

        <div class="divider"></div>

        <div class="text-center footer-text">
            © <?= date('Y') ?> Eastern Visayas State University  
            <br>
            All Rights Reserved | Official Government Portal
        </div>

    </div>
</div>
<script src="https://unpkg.com/html5-qrcode"></script>
<script>
function showVerifySection() {
    document.getElementById('loginSection').style.display = 'none';
    document.getElementById('verifySection').style.display = 'block';
}

function showLoginSection() {
    document.getElementById('verifySection').style.display = 'none';
    document.getElementById('loginSection').style.display = 'block';
}
function showTokenInput() {
    document.getElementById('tokenForm').style.display = 'block';
    document.getElementById('qrScanner').style.display = 'none';
}

function showScanner() {
    document.getElementById('tokenForm').style.display = 'none';
    document.getElementById('qrScanner').style.display = 'block';
}

function showScanner() {
    document.getElementById('tokenForm').style.display = 'none';
    document.getElementById('qrScanner').style.display = 'block';

    const html5QrCode = new Html5Qrcode("reader");

    html5QrCode.start(
        { facingMode: "environment" },
        {
            fps: 10,
            qrbox: 250
        },
        qrCodeMessage => {
            // Auto redirect after scan
            window.location.href = "<?= base_url('payroll/verify_token/') ?>" + qrCodeMessage;
        },
        errorMessage => {}
    );
}
</script>

</body>
</html>